import React from 'react';
import { Default } from '@layouts/index';
import { PortfolioSpecific } from '@view/index';

const PortfolioSpecificPage = () => {
  return (
    <Default>
      <PortfolioSpecific />
    </Default>
  );
};

export default PortfolioSpecificPage;
