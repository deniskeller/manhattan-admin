import React from 'react';
import { Default } from '@layouts/index';
import { ContactUs } from '@view/index';

const ContactUsPage = () => {
  return (
    <Default>
      <ContactUs />
    </Default>
  );
};

export default ContactUsPage;
