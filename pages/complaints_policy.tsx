import React from 'react';
import { Default } from '@layouts/index';
import { ComplaintsPolicy } from '@view/index';

const ComplaintsPolicyPage = () => {
  return (
    <Default>
      <ComplaintsPolicy />
    </Default>
  );
};

export default ComplaintsPolicyPage;
