import React from 'react';
import { Default } from '@layouts/index';
import { CookiePolicy } from '@view/index';

const CookiePolicyPage = () => {
  return (
    <Default>
      <CookiePolicy />
    </Default>
  );
};

export default CookiePolicyPage;
